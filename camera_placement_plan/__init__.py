"""Camera placement geometry + thermal fall-detection algorithm.

Importable so the tracking engine (``tracking_engine.thermal``) can re-use
:class:`thermal_fall_detection.PrivacyThermalFallDetector` without
duplicating logic.
"""
